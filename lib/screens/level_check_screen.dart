import 'package:flutter/material.dart';

class LevelCheckScreen extends StatelessWidget {
  const LevelCheckScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Check-up'));
  }
}
